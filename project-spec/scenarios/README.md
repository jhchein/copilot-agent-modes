# Scenarios

These scenarios are active evaluation inputs for this repository.

They define holdout-style review perspectives used by the `evaluator` agent.

Each scenario should include `Scoring dimensions` as the authoritative evaluation rubric.

If you copy this template to another repo, replace these scenarios with project-specific stakeholders and constraints.
